Q11) A static pod web-serv running on controlplane node on each clusters (cluster1..3) and you need to migrate 
     it on node0 and it should be recreated/restarted automatically in case of failure.
